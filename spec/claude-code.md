# Spec

- Comnmand to turn a Logseq page into a spec that is then processed by claude code
- optionally specify an exiting project (repo).

# Commands

## /PushClaudeCode

(needs work)
needs to take a parameter, that seems hard. A pull-down list including every known repo? (Or page I suppose)

