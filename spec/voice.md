# Spec for voice → Logseq
- Not even sure if this properly belongs in AskClaude, has nothing to do with Claude.
- Spec for voice plugin
- Direct voice->text (and yes this could use Claude)
- Make it easy to bring in Google Voice recordings (from phone). This has its own ->text transformation.

## Commands

### /VoiceInput (this should already work?)
### /PullGoogleVoice
OK name needs improvement...pull any google voice recordings for the particular day (on a journal page)

Optionally the voice as well I suppose. Actually yes. 


