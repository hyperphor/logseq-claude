# Minimal Logseq plugin for Claude

# Installation

Logseq → Settings → Plugins → Load unpacked plugin → select the folder

# Configuration

Edit ~/.logseq/settings/logseq-claude.json per the model in example.

# Use

In Logseq, /Ask Claude



